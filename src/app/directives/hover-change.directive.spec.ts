import { HoverChangeDirective } from './hover-change.directive';

describe('HoverChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverChangeDirective();
    expect(directive).toBeTruthy();
  });
});
